# students height in centimeters
# list
# write a for loop
# len method, sun method
# ignore those, pretend they didn't exist
# for loop
student_heights = input().split()
for n in range(0, len(student_heights)):
    student_heights[n] = int(student_heights[n])
